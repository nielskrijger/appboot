name: unit tests
on: push
jobs:
  unit-tests:
    name: Run unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: test
        uses: cedrickring/golang-action@1.7.0
        with:
          args: make test